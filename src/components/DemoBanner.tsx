export function DemoBanner() {
  return (
    <div className="bg-[#f6ead0] text-[#7a5b12] text-[12.5px] font-bold text-center px-4 py-2">
      Demo mode — sample riders and figures. Nothing here is real, and it resets each time the demo starts.
    </div>
  );
}
